export interface ParticlePoint {
  x: number;
  y: number;
  vx: number;
  vy: number;
  r: number;
  accent: boolean;
}

export interface PointerState {
  x: number;
  y: number;
  active: boolean;
}
